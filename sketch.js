function setup() {
    createCanvas(400, 400);
}

function draw() {
    background(50, 100, 100);
    fill(10, 100, 250);
    ellipse(mouseX, mouseY, 40, 40);
    console.log("Check,Check");
}
